let myTable = [];

let row = 4;
let col = 5;

let counter = 0;

for(let i = 0; i < row; i++){
    let tempTable = [];

    for(let j = 0; j < col ; j++){
         counter++;

         tempTable.push(counter)
    }
    myTable.push(tempTable)
}

console.log(myTable);
